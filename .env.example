# Configurações de Segurança
SECRET_KEY=your-secret-key-here
DEBUG=False

# Database (PostgreSQL no Render ou deixe vazio para SQLite local)
# Para produção:
DATABASE_URL=postgresql://user:password@host:5432/database_name
# Para desenvolvimento local: deixe comentado ou remova a linha

# Hosts permitidos (opcional - já configurado automaticamente)
# ALLOWED_HOSTS=localhost,127.0.0.1

# CORS - Origens permitidas (opcional - já configurado automaticamente)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,https://www.cptec.co.mz

# CSRF - Origens confiáveis (opcional - já configurado automaticamente)
# CSRF_TRUSTED_ORIGINS=https://www.cptec.co.mz

# Variáveis automáticas do Render (não precisa configurar manualmente)
# RENDER=true
# RENDER_EXTERNAL_HOSTNAME=seu-app.onrender.com
# RENDER_INTERNAL_HOSTNAME=seu-app-internal.onrender.com
